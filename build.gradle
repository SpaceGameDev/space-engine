group parent.group
version parent.version

apply plugin: 'java'

properties.putAll parent.properties

configurations {
    implementation.dependencies.addAll rootProject.configurations.implementation.dependencies
    testImplementation.dependencies.addAll rootProject.configurations.testImplementation.dependencies
}

repositories {
    addAll parent.repositories.asCollection()
}

dependencies {
    implementation project(':space-util')
}
